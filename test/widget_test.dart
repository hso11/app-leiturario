// Widget tests are skipped in this project.
// Unit tests are in test/domain/, test/data/, and test/presentation/.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
